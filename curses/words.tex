\section{curses}
\index{curses\$init}\subsection{init}
(optlist --) initialise curses 


\index{curses\$echo}\subsection{echo}
(bool --) set echo state


\index{curses\$cbreak}\subsection{cbreak}
(bool --) set cbreak state


\index{curses\$newline}\subsection{newline}
(bool --) set nl state


\index{curses\$move}\subsection{move}
(x y --) move cursor


\index{curses\$write}\subsection{write}
(s --) write a string


\index{curses\$getch}\subsection{getch}
(-- s) get character


\index{curses\$size}\subsection{size}
(-- y x) get max xy coords


\index{curses\$getpos}\subsection{getpos}
(-- y x) get xy coords


\index{curses\$setcols}\subsection{setcols}
(list --) if colour, set colour pairs

List contains pairs of assignments for colours 1 up to 7,
as symbols: `red=COLOR_RED and so on.
\index{curses\$attrs}\subsection{attrs}
(list -- long) build an attribute list for passing to attrset

This list consists of symbols or integers. If an integer is found, it
is converted to a colour pair attribute, e.g. [`standout,1] is standout
with colour pair 1.
\index{curses\$setattrs}\subsection{setattrs}
(long -- ) set attributes generated by attrs


\index{curses\$refresh}\subsection{refresh}
( -- ) refresh the screen


